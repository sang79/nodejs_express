<div class="mt-4">
    <div class="row">
        <div class="col-lg-3">
            <button class="btn btn-primary">Learn now!</button>
        </div>
        <div class="col-lg-9">
            <h2>{{course.name}}</h2>
            {{!-- thằng videoId lấy trên url của youtu là thằng string nằm sau chữ v?= --}}
            <iframe width="560" height="315" src="https://www.youtube.com/embed/{{course.videoId}}" title="YouTube video player" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
            <p>{{course.description}}</p>
        </div>
    </div>
</div>